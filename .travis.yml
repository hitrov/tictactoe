language: node_js
node_js:
  - "stable"
cache:
  directories:
    - front/node_modules
script:
  - cd front
  - npm run build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: front/build
  on:
    branch: master

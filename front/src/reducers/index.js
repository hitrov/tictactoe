const reducer = (state = {}, action) => {
    switch (action.type) {
        case 'MOVE':
            return state;

        case 'CREATE_PLAYERS':
            return state;

        case 'GAME':
            return state;

        default:
            return state;
    }
};

export default reducer;
